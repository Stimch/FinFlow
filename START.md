# Инструкция по запуску и проверке FinFlow

## Шаг 1: Запуск проекта

### Проверка наличия Docker

Убедитесь, что Docker и Docker Compose установлены:

```bash
docker --version
docker compose --version
```

### Запуск всех сервисов

```powershell
# Перейдите в директорию проекта (если еще не там)
cd c:\Projects\DB_Labs\cp

# Запустите контейнеры
# Для новых версий Docker используйте:
docker compose up -d

# Для старых версий (если команда выше не работает):
docker compose up -d
```

Эта команда:
- Создаст и запустит контейнер PostgreSQL
- Создаст и запустит контейнер backend-приложения
- Автоматически инициализирует базу данных

**Подождите 15-30 секунд** для полной инициализации базы данных.

## Шаг 2: Проверка статуса контейнеров

```powershell
# Проверьте статус контейнеров
docker compose ps

# Или для старых версий:
docker compose ps
```

Должны быть запущены:
- `finflow_db` (PostgreSQL) - статус `Up`
- `finflow_backend` (FastAPI) - статус `Up`

Если контейнеры не запущены, проверьте логи:

```bash
docker compose logs
```

## Шаг 3: Проверка работоспособности API

### 3.1. Проверка health endpoint

Откройте в браузере или выполните в терминале:

```bash
# В браузере
http://localhost:8000/health

# Или через curl (если установлен)
curl http://localhost:8000/health
```

Ожидаемый ответ:
```json
{"status":"healthy"}
```

### 3.2. Проверка корневого endpoint

```bash
# В браузере
http://localhost:8000/

# Или через curl
curl http://localhost:8000/
```

Ожидаемый ответ:
```json
{
  "message": "FinFlow API",
  "version": "1.0.0",
  "docs": "/docs",
  "redoc": "/redoc"
}
```

### 3.3. Открытие Swagger документации

Откройте в браузере:
```
http://localhost:8000/docs
```

Должна открыться интерактивная документация API с возможностью тестирования endpoints.

## Шаг 4: Проверка базы данных

### 4.1. Подключение к базе данных

```bash
docker compose exec db psql -U finflow_user -d finflow_db
```

### 4.2. Проверка таблиц

В psql выполните:

```sql
-- Список всех таблиц
\dt

-- Должно показать 10 таблиц:
-- users, accounts, categories, transactions, tags, 
-- transaction_tags, budgets, goals, recurring_transactions, audit_log
```

### 4.3. Проверка количества записей

```sql
-- Проверка пользователей
SELECT COUNT(*) FROM users;

-- Проверка счетов
SELECT COUNT(*) FROM accounts;

-- Проверка транзакций
SELECT COUNT(*) FROM transactions;

-- Проверка категорий
SELECT COUNT(*) FROM categories;
```

**Без тестовых данных** все должны вернуть `0`.

### 4.4. Проверка функций

```sql
-- Проверка существования функций
\df

-- Должны быть функции:
-- get_user_total_balance, get_transactions_sum, 
-- get_goal_progress, get_budget_exceeded, и др.
```

### 4.5. Проверка представлений (VIEW)

```sql
-- Список представлений
\dv

-- Должно быть 8 представлений:
-- v_user_accounts_summary, v_monthly_financial_summary, и др.
```

### 4.6. Проверка триггеров

```sql
-- Список триггеров
SELECT trigger_name, event_object_table 
FROM information_schema.triggers 
WHERE trigger_schema = 'public'
ORDER BY event_object_table;
```

Должны быть триггеры на таблицах: users, accounts, categories, transactions, budgets, goals, recurring_transactions, tags.

### 4.7. Выход из psql

```sql
\q
```

## Шаг 5: Загрузка тестовых данных (опционально)

Если хотите протестировать систему с данными:

```bash
docker compose exec db psql -U finflow_user -d finflow_db -f /docker-entrypoint-initdb.d/05_seed_data.sql
```

После загрузки проверьте:

```bash
docker compose exec db psql -U finflow_user -d finflow_db -c "SELECT COUNT(*) FROM users;"
# Должно вернуть: 10

docker compose exec db psql -U finflow_user -d finflow_db -c "SELECT COUNT(*) FROM transactions;"
# Должно вернуть: 5000+
```

## Шаг 6: Тестирование API через Swagger

### 6.1. Регистрация пользователя

1. Откройте http://localhost:8000/docs
2. Найдите `POST /api/auth/register`
3. Нажмите "Try it out"
4. Введите данные:
```json
{
  "email": "test@example.com",
  "password": "testpassword123",
  "full_name": "Test User",
  "currency": "RUB"
}
```
5. Нажмите "Execute"
6. Должен вернуться созданный пользователь с `id`

### 6.2. Вход в систему

1. Найдите `POST /api/auth/login`
2. Нажмите "Try it out"
3. Введите:
   - `username`: `test@example.com`
   - `password`: `testpassword123`
4. Нажмите "Execute"
5. Скопируйте `access_token` из ответа

### 6.3. Авторизация в Swagger

1. Нажмите кнопку **"Authorize"** (зеленый замок) в правом верхнем углу
2. В поле "Value" введите: `Bearer YOUR_ACCESS_TOKEN`
   (замените YOUR_ACCESS_TOKEN на токен из предыдущего шага)
3. Нажмите "Authorize"
4. Нажмите "Close"

Теперь все запросы будут автоматически включать токен авторизации.

### 6.4. Создание счета

1. Найдите `POST /api/accounts`
2. Нажмите "Try it out"
3. Введите:
```json
{
  "name": "Основной счет",
  "type": "debit_card",
  "balance": 10000.00,
  "currency": "RUB"
}
```
4. Нажмите "Execute"
5. Запомните `id` созданного счета

### 6.5. Создание категории

1. Найдите `POST /api/categories`
2. Нажмите "Try it out"
3. Введите:
```json
{
  "name": "Продукты",
  "type": "expense",
  "icon": "🛒",
  "color": "#F44336"
}
```
4. Нажмите "Execute"
5. Запомните `id` созданной категории

### 6.6. Создание транзакции

1. Найдите `POST /api/transactions`
2. Нажмите "Try it out"
3. Введите (используйте ID из предыдущих шагов):
```json
{
  "account_id": 1,
  "category_id": 1,
  "amount": 500.00,
  "type": "expense",
  "date": "2024-01-15",
  "description": "Покупка продуктов"
}
```
4. Нажмите "Execute"
5. Должна быть создана транзакция

### 6.7. Проверка автоматического обновления баланса

1. Найдите `GET /api/accounts/{account_id}`
2. Введите ID счета (например, `1`)
3. Нажмите "Execute"
4. Проверьте, что `balance` уменьшился на 500.00 (если был 10000, стало 9500)

### 6.8. Проверка аудита

```bash
docker compose exec db psql -U finflow_user -d finflow_db -c "SELECT table_name, action, changed_at FROM audit_log ORDER BY changed_at DESC LIMIT 10;"
```

Должны быть записи о создании транзакции, обновлении счета и т.д.

### 6.9. Тестирование батчевой загрузки

1. Найдите `POST /api/transactions/batch-import`
2. Нажмите "Try it out"
3. Введите:
```json
{
  "transactions": [
    {
      "account_id": 1,
      "category_id": 1,
      "amount": 100.00,
      "type": "expense",
      "date": "2024-01-16",
      "description": "Транзакция 1"
    },
    {
      "account_id": 1,
      "category_id": 1,
      "amount": 200.00,
      "type": "expense",
      "date": "2024-01-17",
      "description": "Транзакция 2"
    }
  ]
}
```
4. Нажмите "Execute"
5. Должен вернуться отчет с количеством успешных и неудачных операций

### 6.10. Тестирование функций БД

1. Найдите `GET /api/accounts/summary/total-balance`
2. Нажмите "Execute"
3. Должен вернуться общий баланс всех счетов пользователя

### 6.11. Тестирование отчетов

1. Найдите `GET /api/transactions/reports/financial`
2. Нажмите "Try it out"
3. Введите:
   - `start_date`: `2024-01-01`
   - `end_date`: `2024-01-31`
4. Нажмите "Execute"
5. Должен вернуться финансовый отчет по категориям

## Шаг 7: Проверка логов

### Просмотр логов всех сервисов

```bash
docker compose logs -f
```

### Просмотр логов только базы данных

```bash
docker compose logs -f db
```

### Просмотр логов только backend

```bash
docker compose logs -f backend
```

## Шаг 8: Проверка производительности (опционально)

### Анализ запроса

```bash
docker compose exec db psql -U finflow_user -d finflow_db -f /docker-entrypoint-initdb.d/07_performance_queries.sql
```

Или вручную:

```bash
docker compose exec db psql -U finflow_user -d finflow_db
```

```sql
-- Пример анализа запроса
EXPLAIN ANALYZE
SELECT t.*, c.name as category_name, a.name as account_name
FROM transactions t
JOIN accounts a ON t.account_id = a.id
LEFT JOIN categories c ON t.category_id = c.id
WHERE a.user_id = 1
  AND t.date BETWEEN '2023-01-01' AND '2023-12-31'
ORDER BY t.date DESC
LIMIT 100;
```

## Шаг 9: Остановка проекта

```bash
# Остановка контейнеров
docker compose down

# Остановка с удалением данных (ОСТОРОЖНО!)
docker compose down -v
```

## Устранение проблем

### Проблема: Контейнеры не запускаются

**Решение:**
```bash
# Проверьте логи
docker compose logs

# Пересоздайте контейнеры
docker compose down
docker compose up -d --build
```

### Проблема: Порт 5432 или 8000 занят

**Решение:**
Измените порты в `docker compose.yml`:
```yaml
ports:
  - "5433:5432"  # Измените 5432 на другой порт
  - "8001:8000"  # Измените 8000 на другой порт
```

### Проблема: База данных не инициализируется

**Решение:**
```bash
# Удалите том с данными и пересоздайте
docker compose down -v
docker compose up -d

# Или инициализируйте вручную
docker compose exec db psql -U finflow_user -d finflow_db -f /docker-entrypoint-initdb.d/01_schema.sql
```

### Проблема: Backend не может подключиться к БД

**Решение:**
```bash
# Проверьте, что БД запущена
docker compose ps

# Проверьте логи
docker compose logs backend

# Перезапустите backend
docker compose restart backend
```

## Готово!

Если все шаги выполнены успешно, система работает корректно. 

Для дальнейшей работы:
- Используйте Swagger UI: http://localhost:8000/docs
- Читайте документацию: README.md
- Следуйте инструкциям: DEPLOYMENT.md


